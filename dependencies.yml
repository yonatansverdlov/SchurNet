name: weis_dis
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch
      - torchvision
      - torchaudio
      - easydict
      - h5py
      - tensorboard
      - pyyaml
      - tqdm
